/** Barrel per TypeScript; Metro usa `pdf.native.ts` / `pdf.web.ts` a runtime. */
export { generatePdfForMonth, shareFile } from './pdf.native';
export type { ReportMeseData, PdfExportScope, GeneratePdfOptions } from './pdf.native';
