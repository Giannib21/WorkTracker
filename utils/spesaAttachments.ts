/** Barrel per TypeScript; Metro usa `spesaAttachments.native.ts` / `spesaAttachments.web.ts` a runtime. */
export {
  attachmentsDirUri,
  ensureAttachmentsDir,
  persistPickedFile,
  isProbablyImagePath,
} from './spesaAttachments.native';
